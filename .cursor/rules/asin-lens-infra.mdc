---
description: 数据库、迁移与部署；pooler 与 admin migrate
globs: src/lib/db/**/*.ts,drizzle/**/*.sql,vercel.json,.env.example,scripts/apply-schema.mjs,src/app/api/admin/**/*.ts
alwaysApply: false
---

# 基础设施

动手前阅读：`docs/kb/05-infra.md`

## Schema

- 增量：`drizzle/0001_own_competitor.sql`（role、manual_cvr_60d、keyword_traffic、industry_opt_reports）
- `scripts/apply-schema.mjs` 按文件名顺序应用全部 `.sql`

## 连接

- 本机直连 `db.*.supabase.co:5432` 常遇 IPv6 超时
- `DATABASE_URL` 用 Supabase **Transaction pooler**（host 含 `pooler`，端口 6543，用户 `postgres.<ref>`）
- pooler 常拒 DDL：`ensureAutoDailyColumn` 先查列是否存在；Cron / 列表不得把 `ALTER` 当硬前置
- 部署后可用 `POST /api/admin/migrate`（需 ADMIN_TOKEN）打增量
- 日更 Cron：`vercel.json` UTC 10:00/11:00 ≈ 美西 03:00；业务日 `lastCompletedPacificDay`

改 Schema / SQL / 全局 env：**先行动计划，等「确认执行」**。禁止擅自 push。
