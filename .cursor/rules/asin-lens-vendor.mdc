---
description: 外部 MCP 官方 schema 为能力真源（Sif · SellerSprite）
globs: docs/vendor/**/*,src/lib/mcp/**/*.ts,src/lib/research/collect.ts,src/lib/research/keyword-traffic.ts,src/lib/research/normalize.ts
alwaysApply: false
---

# Vendor MCP

## 真源

| 厂商 | 路径 | 站点参数 |
|------|------|----------|
| **Sif** | `docs/vendor/sif-mcp/sif-mcp-tool-schema.json` | **`country`** |
| **SellerSprite** | `docs/vendor/sellersprite-mcp/`（`README.md` + `sellersprite-mcp-tools.json` + 全文 `sellersprite-mcp-api.md`） | **`marketplace`** |

- 改工具名、入参、返回字段映射前 **必须先读对应真源**
- 禁止调用目录/schema 中不存在的工具
- 禁止把未声明指标写成「已返回」（尤其 ASIN×词 CVR）
- **禁止混用站点参数名**：Sif=`country`，SS=`marketplace`

## SellerSprite 常用对齐（摘要）

| 需求 | 工具 | 说明 |
|------|------|------|
| 标题/价/BSR | `asin_detail` | 采集主源 |
| 词 searches / bid | `traffic_keyword` | 补缺；`purchaseRate` **不入** cvr |
| FBA / 包装重 | `keepa_info` | 可选 → `rawRefs.listingExtras`；趋势数组勿盖标量售价 |
| 关键词流向 | `traffic_source` | ≠ Listing 自然/广告结构 |

认证：`secret-key`。完整 43 工具见 `sellersprite-mcp-tools.json`。

## Sif 常用对齐（摘要）

| 需求 | 工具 |
|------|------|
| 主词/份额 | `market_get_asin_keyword_signals` |
| 流量来源（自然 vs 广告） | `ops_get_listing_traffic_overview` |
| 词搜索量（市场） | `market_get_keyword_history`（非 ASIN 词 CVR） |

禁止调用 Sif 不存在的工具（如 `asin_detail`）。
