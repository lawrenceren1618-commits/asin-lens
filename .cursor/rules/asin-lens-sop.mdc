---
description: ASIN Lens 架构守则与开发 SOP（全局强制）
alwaysApply: true
---

# ASIN Lens 行为守则

## 模块树（一眼看 · 仅开发自用）

> **只给写代码的人看进度 / 对号入座用。**  
> 禁止挂到客户前端、禁止当产品功能展示。对照存在于本规则 + `AGENTS.md` + `docs/kb/INDEX.md`。

| 区块 | kb | 职责 | 开聊声明 |
|------|-----|------|----------|
| **contracts** | `01-contracts` | 角色 own/竞品 · keywordTraffic · 双报告契约 | `mod/contracts` |
| **frontend** | `02-frontend` | 我的/竞品 UI · 氛围花语 | `mod/frontend` |
| **collect** | `03-collect` + vendor | SS+Sif 采集 · 清洗核对 | `mod/collect` |
| **report** | `04-report` | 异动日报 ≠ 行业/优化报告 | `mod/report` |
| **infra** | `05-infra` | Schema · pooler · migrate | `mod/infra` |
| **vendor** | `docs/vendor/sif-mcp` | Sif 官方 schema，禁止臆造字段 | `mod/vendor` |

进度摘要：`docs/kb/07-changelog-*.md` · 全索引：`docs/kb/INDEX.md`

## 产品中心

- **我的 ASIN 可选**（可多变体）：无 own → 仅行业竞品报告；有 own → 行业报告 + 可优化项
- **异动日报** 与 **行业/优化报告** 分离，禁止混进同一契约
- 外部 MCP 能力以 `docs/vendor/**` 官方 schema 为准，禁止臆造字段

## 四大区（改代码前先定位）

1. **前端** `src/components/**`, `src/app/**/page.tsx`
2. **后端** `src/lib/research/**`, `src/app/api/**`, `src/lib/notify/**`
3. **基础设施** `src/lib/db/**`, `drizzle/**`, `vercel.json`, `.env*`
4. **交叉契约** `docs/kb/01-contracts.md` + types — 禁止凭空重写

## SOP

1. 全局思路 → 2. 拆模块 → 3. 定接口 → 4. **确认后再写核心代码**

## 确认机制（最高优先级）

涉及 Schema/SQL、全局配置、跨模块接口：先行动计划 → 等 **「确认执行」** → 再改。

## 准备上新（对话交接）

用户说 **「准备上新」** / **「上新」** / **「换对话」** 时：

1. **估上下文占用**：综合本对话轮次、长工具输出、已读大文件等；若 **约 ≥70%**（或已明显吃紧）→ 明确告知「建议关闭本对话，用下方模板开新聊」
2. **等任务**：用户给出下一任务（一句即可）
3. **拆模块**：对照上表，列出要动的 `mod/*`、必读 `docs/kb/*.md`、关键 `.cursor/rules/*.mdc`、关键源码路径（3～8 个关键文件即可）
4. **自动生成开聊模板**（可直接复制到新对话），格式固定为：

```
按 ASIN Lens 守则（全局 SOP）。
本区块：mod/<主模块>（兼：mod/<次要>…）
必读：docs/kb/01-contracts.md + docs/kb/<…>
可选：docs/vendor/…（若涉及 Sif）
背景摘要：<上轮结论 2～4 句 + 指向 07-changelog>
本轮目标：<用户任务改写为一句可执行目标>
先给行动计划，等我「确认执行」后再改代码。
我说 push = 只上传影响前端使用效果的代码；规则/kb 本地入库即可。
```

5. 模板末可附「建议先读文件」清单；**不要**在交接模板里塞大段代码。

## 洁癖底线（每个任务都生效）

来源：个人 skill `neat-freak`（说「洁癖」可走完整收尾）。日常开发只守这些，不打断主任务：

- **单一真相**：契约 / Sif 字段 / 双报告边界以 kb + 代码 + vendor schema 为准，禁止臆造  
- **改完能对上**：实质改动后，相关 `docs/kb/*` 与 `07-changelog-*.md`、规则若过期则同步（契约级仍先确认）  
- **先减后加**：不堆平行过期说法；历史进 changelog，现役只留一处权威  
- **不擅自清场**：删除分支、临时文件、旧副本前先列清单等确认  
- **状态分清**：本地已 commit ≠ 已 push 远程 ≠ 生产已验证  

完整收尾流程见 skill：`~/.cursor/skills/neat-freak/SKILL.md`（触发词：洁癖 / neat-freak / 收尾同步文档）。

## Git（本地入库 vs 远程上传）

### 分流原则（最高优先级）

| 改动类型 | 本地 commit | `git push` 远程 |
|----------|-------------|-----------------|
| **影响前端使用效果**（用户看得见/用得着） | ✅ | ✅ 用户说 push 时上传 |
| **不影响功能**（规则、kb、AGENTS、洁癖、模块树、纯文档、Agent 卫生） | ✅ 进本地仓即可 | ❌ **默认不上传** |

**算「影响前端使用效果」**（可 push）：

- `src/app/**`、`src/components/**`、`src/app/globals.css`、`public/**` 等用户界面与静态资源  
- 前端会调用且改变行为的 `src/app/api/**`、报告/采集管线中**改变页面展示或按钮结果**的逻辑  
- 为让上述前端改动能跑起来所必需的 schema/迁移（须仍走确认机制）

**不算**（只本地 commit，除非用户明确说「连文档一起 push / 全量 push」）：

- `.cursor/rules/**`、`AGENTS.md`、`docs/kb/**`、纯注释/排版、个人 skill、开发提醒用模块树  

> Cursor 读的是**本机**规则与 kb；对话变好不依赖把它们推到 GitHub。远程只服务部署与他人可见的产品效果。

### 用户说「入库」/ 收尾有本地改动时

1. 该补的 `07-changelog-*.md` 先补（可只本地）  
2. `git commit` 全部相关改动（勿提交 `.env`）  
3. **到此为止**，除非同时满足「有前端效果改动」且用户说了 **push**

### 用户说「push」时

1. 检查待推送提交 / 工作区：是否含「影响前端使用效果」的文件  
2. **有** → changelog（若需要）→ 确保已本地 commit → `git push`（禁止 force push）→ 回报 hash  
3. **仅有文档/规则/洁癖类** → **不要 push**；改为本地 commit（若尚未入库），并明确告知「已本地入库，未上传远程（不影响前端）」  
4. 若一次提交里**混有**两类：优先拆开——前端效果单独 commit 再 push；文档/规则另 commit 只留本地。无法拆时先问用户，默认只 push 前端相关 commit  

其它约定：

- 分支名：`mod/<模块>`，与模块树对齐  
- 单点通过后可提醒「建议本地入库」；未说 push 则不上传  
