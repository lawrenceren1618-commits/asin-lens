---
description: ASIN Lens 业务守则 — 模块组成与跨模块影响（可进远程仓）
alwaysApply: true
---

# ASIN Lens 业务守则

> **本文件描述业务组成部分 + 组成部分之间的影响规则**（技能运转前提）。须进本地仓并随业务一并 **push**。  
> **对话理解 / 省上下文**（准备上新、洁癖流程、push 话术等）在本机：`~/.cursor/rules/asin-lens-chat-hygiene.mdc`，**不进远程**。

## 模块树（组成部分）

> 开发对号入座用，**不是客户 UI**。

| 区块 | kb | 职责 | 开聊声明 |
|------|-----|------|----------|
| **contracts** | `01-contracts` | 角色 own/竞品 · keywordTraffic · 双报告契约 | `mod/contracts` |
| **frontend** | `02-frontend` | 我的/竞品 UI · 氛围花语 | `mod/frontend` |
| **collect** | `03-collect` + vendor | SS+Sif 采集 · 清洗核对 | `mod/collect` |
| **report** | `04-report` | 异动日报 ≠ 行业/优化报告 | `mod/report` |
| **infra** | `05-infra` | Schema · pooler · migrate | `mod/infra` |
| **vendor** | `docs/vendor/sif-mcp` | Sif 官方 schema，禁止臆造字段 | `mod/vendor` |

进度摘要：`docs/kb/07-changelog-*.md` · 全索引：`docs/kb/INDEX.md`

## 产品中心（跨模块影响）

- **我的 ASIN 可选**（可多变体）：无 own → 仅行业竞品报告；有 own → 行业报告 + 可优化项
- **异动日报** 与 **行业/优化报告** 分离，禁止混进同一契约
- 外部 MCP 能力以 `docs/vendor/**` 官方 schema 为准，禁止臆造字段

## 四大区（改代码前先定位）

1. **前端** `src/components/**`, `src/app/**/page.tsx`
2. **后端** `src/lib/research/**`, `src/app/api/**`, `src/lib/notify/**`
3. **基础设施** `src/lib/db/**`, `drizzle/**`, `vercel.json`, `.env*`
4. **交叉契约** `docs/kb/01-contracts.md` + types — 禁止凭空重写

## SOP

1. 全局思路 → 2. 拆模块 → 3. 定接口 → 4. **确认后再写核心代码**

## 确认机制（最高优先级）

涉及 Schema/SQL、全局配置、**跨模块接口**：先行动计划 → 等 **「确认执行」** → 再改。

## 远程应包含的业务知识（技能前提）

下列变更 **本地 commit + 用户说 push 时上传**（保证换机/技能仍能运转）：

- 各模块源码与必要配置  
- `docs/kb/00`–`05`、`01-contracts` 等业务说明与跨模块契约  
- `docs/vendor/**`、业务相关 `.cursor/rules/asin-lens-{contracts,collect-report,frontend,infra,vendor}.mdc`  
- 本文件 `asin-lens-sop.mdc`、业务向 `AGENTS.md` / `07-changelog-*.md`  
