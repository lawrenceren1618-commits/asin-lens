---
description: 采集与报告管线；打开业务文件时加载对应 kb
globs: src/lib/research/**/*.ts,src/app/api/collect/**/*.ts,src/app/api/cron/**/*.ts,src/app/api/reports/**/*.ts,src/app/api/projects/**/industry-opt-reports/**/*.ts,src/lib/mcp/**/*.ts
alwaysApply: false
---

# 采集 / 报告

动手前阅读：`docs/kb/01-contracts.md` + `03-collect.md` + `04-report.md`  
Sif 边界：`docs/vendor/sif-mcp/sif-mcp-tool-schema.json`

## 单 ASIN 采集链

1. SS `asin_detail`（`marketplace`）→ 标题/价/BSR  
2. SS `traffic_keyword` → searches / bid  
3. SS `keepa_info`（可选）→ FBA / 包装重 → `rawRefs.listingExtras`；趋势数组勿覆盖标量售价  
4. Sif `market_get_asin_keyword_signals`（`country`，`time_type=lately`，`time_value=7`）  
5. Sif `ops_get_listing_traffic_overview`（`country`，`timePieceType=latelyDay`）  
6. clean → verify → 写 `asin_snapshots`（含 `keyword_traffic` + `listingExtras`；日期 = 美西已过完的一天）  
7. `planChangeLogWrite`：缺口日后有变化 → 旧段不动、插新 `asin_change_log`

Sif **无** `asin_detail`。入参用 **`country`**，勿传 `marketplace`。

## 报告

- 异动日报：Cron / `generateDailyReports`（**不含**单位经济）  
- 行业/优化：`POST .../industry-opt-reports`（可带 `commerceRates`）；含 `unitEconomics`；分散型不深挖；CVR/竞价/花费按契约 no_result  

遵守：清洗→核对→入库；导出前核对→再格式化。
